# Publish service ports to the host so host-run pytest integration tests can reach them.
services:
  postgres:
    ports:
      - "5432:5432"
  elasticsearch:
    ports:
      - "9200:9200"
  redis:
    ports:
      - "6379:6379"
