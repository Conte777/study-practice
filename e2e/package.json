{
  "name": "e2e",
  "private": true,
  "version": "0.1.0",
  "description": "QA-02 — Playwright E2E for the knowledge-search critical path",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0"
  }
}
