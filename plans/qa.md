# План доделок: QA

**Ветка:** `feat/qa-real-metrics`
**Роль:** тесты, тестовые данные, метрики качества, документация.

**Что не сделано:** гарнесы **QA-04** (нагрузка) и **QA-05** (Precision@3) есть, но их отчёты
написаны против **mock-бэкенда** (`/search` без ES/Redis). Бэкенд с ES/Redis уже реализован
(коммит `3d5f493`) → метрики нерелевантны, нужно перегнать против настоящего стека и обновить отчёты.

Гейт = ① реальный прогон против поднятого `docker compose` ② обновлённый REPORT.md с настоящими цифрами.

**Рабочий процесс:**
- После каждой пройденной стадии — коммит через `mcp__git__commit` (если есть что коммитить).
- После выполнения всего плана — `git push -u HEAD` и создать PR по шаблону
  `.github/pull_request_template.md` (`gh pr create`).

---

## Стадия 0 — Предусловие: реальные данные

**Задачи**
- Поднять стек: `docker compose up -d`.
- Засеять документы (см. DO-07 `init.sh`) ИЛИ загрузить `tests/fixtures/ok.pdf`, `ok.docx` + лекции.
- Дождаться `status=indexed` у всех.

**Гейт 0**
- `GET /search?q=<термин>` возвращает непустую выдачу из ES (не mock).

---

## Стадия 1 — QA-04: нагрузка на реальном бэке

**Задачи**
- Прогнать `tests/load/loadtest.py` против настоящего `/search` (50 users):
  `locust -f tests/load/loadtest.py --headless -u 50 -r 10 -t 1m --host http://localhost:8000`.
- Убедиться, что `hot` (повтор → cache-hit Redis) и `unique` (cache-miss → ES) дают **разную** латентность
  (в mock были идентичны — см. текущий REPORT).
- Обновить `tests/load/REPORT.md`: реальные p50/p95/p99, RPS, error%, и **наблюдаемый эффект кеша**
  (разрыв hot vs unique). Убрать оговорку про mock.

**Гейт 1**
- REPORT.md содержит реальные цифры + вывод про Redis-кеш.
- Чеклист: [ ] 50 users [ ] 0 (или объяснённые) ошибки [ ] hot<unique по латентности [ ] отчёт обновлён.

---

## Стадия 2 — QA-05: Precision@3 на реальном ранкере

**Задачи**
- Убедиться, что 10 эталонных документов из `precision_at_3.py` (`ml-lecture.pdf`, `schedule.pdf`,
  `scholarship.docx`, `thesis-guide.pdf`, `library.pdf`, `registration.docx` и др.) **реально загружены и проиндексированы**
  (при необходимости — сгенерировать через `tests/fixtures/generate.py` с релевантным текстом и залить).
- Прогнать `HOST=http://localhost:8000 python tests/quality/precision_at_3.py`.
- Обновить `tests/quality/REPORT.md`: реальная таблица позиций + метрика. Убрать оговорку «mock upper bound».

**Гейт 2**
- Precision@3 посчитан по настоящей выдаче ES (не фиксированная пара `ok.pdf/ok.docx`).
- Чеклист: [ ] 10 док-тов в индексе [ ] 10 запросов отработали [ ] таблица реальная [ ] отчёт обновлён.

---

## Стадия 3 — Регресс юнит/E2E + документация

**Задачи**
- `cd backend && uv run pytest -q --cov=app` → зелёный, покрытие ≥ 50%.
- `cd e2e && npx playwright test` против поднятого стека → критический путь зелёный.
- Сверить `docs/user-guide.md` с фактическим UI (после доделок FE — прогресс-бар); обновить при расхождении.

**Гейт 3**
- Юнит + E2E зелёные, оба REPORT.md обновлены, user-guide актуален.
- Ревью: `review-code` — метрики воспроизводимы (команда прогона в отчёте), нет hardcoded-цифр без прогона.
